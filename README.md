# CafeId

Cafe management dashboard — auth, orders, and admin views for running a cafe.

## Stack
CodeIgniter 4 (PHP)

## Run locally
```bash
composer install
cp env .env
php spark serve
```

Configure database credentials in `.env` before running.
